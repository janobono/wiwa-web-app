export interface ResourceUi {
    confirm: {
        title: string,
        message: string,
        error: string
    },
    contactInfo: {
        businessId: string,
        taxId: string,
        vatRegNo: string
    }
}
