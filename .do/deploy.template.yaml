spec:
  name: wiwa-web-app
  services:
    - build_command: yarn build
      environment_slug: node-js
      git:
        branch: main
        repo_clone_url: https://github.com/janobono/wiwa-web-app.git
      name: wiwa-web-app
